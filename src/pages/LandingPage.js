import React from 'react';
import {Form} from '../components';

const LandingPage = () => {
    return (
        <main className='main'>
            <Form/>
        </main>
    );
};

export default LandingPage;